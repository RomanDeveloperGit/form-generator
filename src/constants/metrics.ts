export enum MetricContext {
  Form = 'Form',
  Field = 'Field',
}

export enum MetricAction {
  Add = 'Add',
  Edit = 'Edit',
  Remove = 'Remove',
  View = 'View',
}

export enum MetricType {
  Success = 'Success',
  Fail = 'Fail',
}
