export enum MetricContext {
  Form = 'Form',
  Field = 'Field',
}

export enum MetricAction {
  View = 'View',
  Create = 'Create',
  Edit = 'Edit',
  Delete = 'Delete',
  MassDelete = 'MassDelete',
}

export enum MetricType {
  Success = 'Success',
  Fail = 'Fail',
}
