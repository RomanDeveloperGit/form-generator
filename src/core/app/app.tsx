import { Forms } from '@/modules/forms';
import { Notifications } from '@/modules/notifications';

export const App = () => {
  // zod
  // react hook form
  // listener
  // sentry
  // local storage, persist???
  // api ???
  // eslint sort imports

  return (
    <div>
      <Forms />
      <Notifications />
    </div>
  );
};
